# Milestone 6: add this provider to the required_providers block in
# infra/versions.tf (needed by github-oidc.tf's tls_certificate data source).

# required_providers {
#   ...
#   tls = {
#     source  = "hashicorp/tls"
#     version = "~> 4.0"
#   }
# }
